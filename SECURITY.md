# 安全说明

本项目是面向单机 DGX Spark、可信内网的社区部署方案，不是经过独立安全审计的托管服务。
公开源码不代表可以直接把默认 API 映射到公网，也不代表依赖和模型文件不存在安全风险。

## 部署边界

- 默认只监听宿主机 `127.0.0.1`。通过出片验收后，才按需绑定实际内网地址。
- 网关使用共享 Bearer API key，没有用户隔离、细粒度权限或每用户配额。
- `/v1/*` 与 `/ready` 需要密钥；`/health`、`/docs`、`/openapi.json` 是公开端点。
- 服务使用 HTTP。不要在不可信网络明文发送密钥、提示词、输入素材或输出视频。
- 有队列/上传大小限制，但不是完整的防滥用或抗拒绝服务系统。
- 上传素材交由媒体解析库和 ffmpeg 处理；不要把任意匿名上传直接交给 GPU 服务。
- 推理进程默认串行，不要同时启动生产、旧服务和 benchmark 占用同一 GPU。

公网或多用户使用前，需要额外设计 HTTPS、网络访问控制、身份/授权隔离、限流与配额、
上传检查、磁盘保留策略和审计。不要仅靠隐藏 URL 或一个共享密钥承担这些职责。

## 密钥、证书和数据

- 真实 `.env`、HF token、API key、私钥、公共企业 CA、模型、用户素材、任务库和镜像备份不提交 Git。
- `.gitignore` 只是防误操作；它不能保护已经提交的文件，也不能识别代码中硬编码的秘密。
- 仅导入由 IT 等可信渠道确认过的公共 CA；不关闭 TLS 校验，不提交 PFX/P12 或私钥。
- 含企业 CA 的镜像应内部保存。Docker 镜像和运行时日志也需要独立检查，不能因为源码已脱敏就直接公开。
- 分享日志、benchmark 摘要、截图或报错前，检查其中的提示词、文件路径、地址和用户内容。
- 如果秘密进入提交、issue 或发布附件，立即轮换/吊销。只删除当前文件不能撤回已泄露的秘密或 Git 历史。

## 漏洞报告

不要在公开 issue 上传密钥、企业证书、完整 `.env`、用户视频或包含这些内容的日志。
如果托管平台启用了私人安全报告，请优先使用；否则先联系仓库维护者建立私密沟通渠道。
未确认有安全的私密渠道前，不发送敏感复现资料。本项目不承诺固定响应或修复时限。
