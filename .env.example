# production/manage.py prepare or scripts/bootstrap_host.py creates the actual .env.
# This is a placeholder, NOT a usable API key. Do not commit your real .env.
H3_API_KEY=
# Local-only acceptance first; then change to the actual DGX LAN IP.
H3_BIND_IP=127.0.0.1
H3_PORT=8000
H3_LOAD_TIMEOUT=2700
H3_TASK_TIMEOUT=7200
